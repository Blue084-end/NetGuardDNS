<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="eu.faircode.netguard">

    <!-- Quyền truy cập mạng và dịch vụ -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- NOTE: WRITE_EXTERNAL_STORAGE removed. Use Storage Access Framework (SAF) or MANAGE_EXTERNAL_STORAGE when necessary. -->

    <application
        android:allowBackup="true"
        android:label="NetGuardDNS"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.NetGuardDNS">

        <!-- Activity chính -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Dịch vụ VPN -->
        <service
            android:name=".NetGuardService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:exported="true"
            android:foregroundServiceType="vpn|network" />

        <!-- Dịch vụ DNS Filter -->
        <service
            android:name=".dnsfilter.DnsServer"
            android:exported="false"
            android:foregroundServiceType="network" />

        <!-- Dịch vụ ghi log -->
        <service
            android:name=".LogService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Dịch vụ giám sát kết nối -->
        <service
            android:name=".ConnectivityService"
            android:exported="false"
            android:foregroundServiceType="network" />

        <!-- BroadcastReceiver khởi động máy -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>